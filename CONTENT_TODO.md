# Final Personalization Checklist

1. Verify all publication titles/authors in `index.html`.
2. Add direct paper links where available (currently linked to Google Sites profile page).
3. Replace `papers/images/pub-placeholder.svg` with actual publication thumbnails if wanted.
4. Push updates:
   - `git add .`
   - `git commit -m "Refine homepage details"`
   - `git push`
